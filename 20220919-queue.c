#include <stdio.h>

const Max_Size = 25;

struct Queue{
    int front;
    int rear;
    int array[Max_Size];
}


Queue init(){
    Queue temp;
    temp.front = -1;
    temp.rear = -1;
}


int enqueue(Queue* queue){
    if(queue.front == queue.rear == )    
}


int dequeue(Queue* queue){
    if
}